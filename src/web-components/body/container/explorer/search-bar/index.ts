// import { lazy } from 'react';
// import withSuspense from '../../../../common/suspense';

// const SearchBar = withSuspense(lazy(() => import('./SearchBar')));

// export default SearchBar;

import SearchBar from './SearchBar';

export default SearchBar;
